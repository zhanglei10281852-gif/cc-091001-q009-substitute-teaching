# 突发缺岗代课接续

教务值班场景使用本项目统一缺岗事件、候选邀请和接续结果的数据名称。`fixtures/absence-context.json` 给出一段脱敏课表信息，`src/domain.js` 定义事件、邀请和确认阶段。邀请有效期按照绝对时间判断，课程时段采用学校所在地的明确时区偏移量。

项目面向 Node.js 20 或更高版本，执行 `npm test` 可以核对样例。真实课表与教师联系信息须由运行环境提供。
